# URL do Web App do Google Apps Script que recebe os leads do formulário de
# contato e da newsletter e grava numa planilha do Google Sheets.
# Veja o passo a passo em LEADS_SETUP.md.
#
# Copie este arquivo pra .env.local (que já é ignorado pelo git) e cole a
# URL gerada no passo 4 do guia.
VITE_LEADS_WEBHOOK_URL=
